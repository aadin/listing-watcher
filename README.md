# Generic Listing Watcher

A plugin-based watcher that polls a data source, remembers seen items,
and sends Discord notifications.

Replace `sources/example_source.py` with your own data source that returns
normalized items.

Run:
```
pip install -r requirements.txt
python main.py
```
